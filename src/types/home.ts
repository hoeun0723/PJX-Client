export type Feel= {
    date: string;
    reactionType: string;
};

export type FeelList = {
    month: string;
}

export type DayPaid = {
    date: string;
}

export type MonthGoal = {
    newGoal: number;
}